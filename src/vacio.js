/**
 * Módulo vacío.
 *
 * jsPDF importa html2canvas y dompurify para su método `.html()`, que convierte
 * HTML a imagen. Este proyecto arma el PDF con autoTable, así que esas
 * dependencias son 226 KB de código que nunca se ejecuta. Vite las sustituye
 * por este stub (ver `resolve.alias` en vite.config.js).
 */
export default {};
